<h1>Eding Book</h1>

<% if @book.errors.any? %>
  <div id="error_explanation" ,"field_with_errors">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>


<%= form_with model: @book, url: book_path(@book), method: :patch do |f|  %>

<div class="field"><h4>Title</h4>
<%= f.text_field :title %></div>

<div class="field"><h4>Body</h4>
<%= f.text_area :body %></div>

<div class="field"><%= f.submit 'Update Book' ,class: 'btn' %></div>


<% end %>


<%= link_to "Show", book_path(@book),class:"show" %>
|
<a class="back" href="/books">Back</a>